Run the test-validator agent to check that all recent changes have proper test coverage and pass quality gates.

Context for what was changed: $ARGUMENTS

The agent should check test coverage, run tests, and run clippy. If anything fails, fix the issues before proceeding.
